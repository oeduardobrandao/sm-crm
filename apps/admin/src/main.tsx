import React from 'react';
import ReactDOM from 'react-dom/client';
import './globals.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>Admin app loading...</div>
  </React.StrictMode>
);
